import React from "react";
import { observer } from "mobx-react";

function SettingsOverlay() {
  return <div style={{ backgroundColor: "white", width: 512, height: 512 }} />;
}

export default observer(SettingsOverlay);
