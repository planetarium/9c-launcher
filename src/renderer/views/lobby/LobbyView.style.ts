import { makeStyles } from "@material-ui/core";

const lobbyViewStyle = makeStyles({
  root: {
    color: "#FFFFFF",
  },

  activation: {
    display: "block",
    marginLeft: "auto",
    marginRight: "auto",
    marginTop: "20px",
    width: "200px",
  },

  verifing: {
    textAlign: "center",
  },

  gameStartButton: {
    marginTop: "65px",
  },
});

export default lobbyViewStyle;
