import { makeStyles } from "@material-ui/core";

const errorViewStyle = makeStyles({
  root: {
    color: "#FFFFFF",
    padding: "24px",
  },
  title: {
    fontWeight: "bold",
    fontSize: "1.5em",
  },
});

export default errorViewStyle;
