import { makeStyles } from "@material-ui/core";

const revokeAccountViewStyle = makeStyles({
  root: {
    margin: "20px",
    color: "white",
  },
  title: {
    display: "block",
    fontWeight: "bold",
    lineHeight: 1.25,
    marginBottom: "5px",
  },
  revoke: {
    marginTop: "100px",
  },
});

export default revokeAccountViewStyle;
