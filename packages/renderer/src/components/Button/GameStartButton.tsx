import {Button} from '@chakra-ui/react';

export function GameStartButton() {
  return (
    <Button
      colorScheme="yellow"
      sx={{
        width: '156px',
        height: '48px',
        fontSize: '1.25rem',
      }}
    >
      Game Start
    </Button>)
}
